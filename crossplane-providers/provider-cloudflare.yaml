# Cloudflare provider is disabled — no production-ready xpkg release exists.
#
# Evaluated options (2026-04-03):
#   - wildbitca/provider-upjet-cloudflare: 0 stars/forks, experimental, CEL bug
#   - crossplane-contrib/provider-cloudflare: archived, no xpkg release
#   - crossplane-contrib/provider-upjet-cloudflare: official successor, 251 CRDs,
#     actively maintained — but no versioned release published yet
#   - cdloh/provider-cloudflare: abandoned since 2024
#
# Re-enable when crossplane-contrib/provider-upjet-cloudflare publishes a release:
#   https://github.com/crossplane-contrib/provider-upjet-cloudflare/releases
#
# apiVersion: pkg.crossplane.io/v1
# kind: Provider
# metadata:
#   name: provider-cloudflare
# spec:
#   package: xpkg.upbound.io/crossplane-contrib/provider-upjet-cloudflare:<version>
#   packagePullPolicy: IfNotPresent
#   revisionActivationPolicy: Automatic
#   runtimeConfigRef:
#     name: default
