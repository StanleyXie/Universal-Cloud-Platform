# Cloudflare provider — disabled, waiting for official release.
#
# Status (2026-04-04):
#   No production-ready xpkg exists in the Crossplane ecosystem.
#
#   wildbitca/provider-upjet-cloudflare: multiple CRD generation defects
#     beyond the original CEL bug — missing Snippet CRD, kind/plural naming
#     mismatches. Not suitable for use without full Upjet code regeneration.
#
#   crossplane-contrib/provider-upjet-cloudflare: official successor, actively
#     maintained, but no versioned release or published xpkg yet.
#     Monitor: https://github.com/crossplane-contrib/provider-upjet-cloudflare/releases
#
# Re-enable when crossplane-contrib publishes a release:
#
# apiVersion: pkg.crossplane.io/v1
# kind: Provider
# metadata:
#   name: provider-cloudflare
# spec:
#   package: xpkg.upbound.io/crossplane-contrib/provider-upjet-cloudflare:<version>
#   packagePullPolicy: IfNotPresent
#   revisionActivationPolicy: Automatic
#   runtimeConfigRef:
#     name: default
