apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Gateway API standard channel CRDs (HTTPRoute, Gateway, GatewayClass, ReferenceGrant)
# v1.2.1 — stable, compatible with k8s 1.28+
resources:
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.2.1/standard-install.yaml
  - gateway.yaml
